{
    email {$CADDY_EMAIL}
}

{$DOMAIN} {
    encode zstd gzip

    @api path /api/* /healthz
    handle @api {
        reverse_proxy backend:8080
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
