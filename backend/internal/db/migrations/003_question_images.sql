-- Поддержка картинок у вопросов.
-- Один файл на вопрос (изображение хранится на диске, в БД лежит относительный URL).
-- Миграция идемпотентна — гоняется на каждом старте.

ALTER TABLE questions
    ADD COLUMN IF NOT EXISTS image_url TEXT;
